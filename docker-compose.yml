version: '3.3'

services:
  postgres-eventhub:
    image: postgres
    restart: always
    environment:
      POSTGRES_DB: "eventhub-api-db"
      POSTGRES_PASSWORD: "postgres"
    ports:
      - "15432:5432"
